# Base URL for the delphi-creator-api Cloudflare Worker that serves
# the mainnet D1 market data powering the Market data overlay.
# Defaults to the production worker; override in CI / preview deploys
# to point at a staging worker.
VITE_DELPHI_CREATOR_API_URL=https://delphi-creator-api.gensyn.workers.dev
